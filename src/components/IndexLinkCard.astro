---
const { title, rotation, description } = Astro.props;
---

<div class="rounded-lg border-2 border-gray-200 p-5 mb-3 bg-white w-full" style={`transform: rotate(${rotation}deg);`}>
    <p class="text-lg">{title}</p>
    <p class="text-md text-gray-600">{description}</p>
</div>
