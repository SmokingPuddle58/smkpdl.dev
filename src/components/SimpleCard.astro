---
import "../styles/global.css";
const { img,rotate, Class } = Astro.props;
---

<div class={`rounded-lg border-2 border-gray-200 p-2 w-20 h-20 ${Class} bg-white`} style={`transform: rotate(${rotate}deg);`}>
    <img src={img} alt="Card Image" />
</div>